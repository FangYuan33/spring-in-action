spring:
  application:
    name: taco-cloud
  cloud:
    config:
      profile: datasource,local

# 开放actuator请求路径
management:
  health:
    jms:
      enabled: off
  endpoints:
    web:
      exposure:
        include: "*"